# text-to-image-generation
Overview
This project generates images from text descriptions using deep learning. The model takes a user-inputted text prompt and produces a corresponding image, which is saved in a local folder.

Features
✅ Converts text prompts into images
✅ Automatically saves generated images
✅ Uses deep learning models for image generation
